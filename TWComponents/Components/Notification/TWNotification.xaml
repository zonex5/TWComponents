<UserControl x:Class="TwComponents.Components.Notification.TwNotification"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" mc:Ignorable="d"
             MaxHeight="100" MaxWidth="400"
             MinHeight="50"
             d:DesignHeight="50" d:DesignWidth="300">
    <UserControl.Resources>

        <Style TargetType="Border">
            <Setter Property="BorderBrush"
                    Value="{Binding NotificationColor.BorderColor, RelativeSource={RelativeSource AncestorType={x:Type UserControl}}}" />
            <Setter Property="Padding" Value="2" />
            <Setter Property="Margin" Value="0 1 0 0" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="4" />
            <Setter Property="Background">
                <Setter.Value>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop
                            Color="{Binding NotificationColor.BackgroundPrimaryColor, RelativeSource={RelativeSource AncestorType={x:Type UserControl}}}"
                            Offset="0" />
                        <GradientStop
                            Color="{Binding NotificationColor.BackgroundSecondaryColor, RelativeSource={RelativeSource AncestorType={x:Type UserControl}}}"
                            Offset="1" />
                    </LinearGradientBrush>
                </Setter.Value>
            </Setter>
        </Style>

        <!--simple button-->
        <Style x:Key="CloseButtonStyle" TargetType="Button">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="Transparent" Margin="0">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"
                                              RecognizesAccessKey="True" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Foreground"
                                        Value="{Binding NotificationColor.BorderColor, RelativeSource={RelativeSource AncestorType={x:Type UserControl}}}" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Foreground"
                                        Value="{Binding NotificationColor.PrimaryColor, RelativeSource={RelativeSource AncestorType={x:Type UserControl}}}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="Foreground"
                    Value="{Binding NotificationColor.PrimaryColor, RelativeSource={RelativeSource AncestorType=UserControl}}" />
            <Setter Property="FontSize" Value="8" />
            <Setter Property="FontFamily" Value="Segoe MDL2 Assets" />
            <Setter Property="FontWeight" Value="Bold" />
        </Style>

    </UserControl.Resources>
    <Border>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="20" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="40" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="20" />
            </Grid.ColumnDefinitions>

            <!--close button ✖️ -->
            <Button Grid.Column="2" Style="{StaticResource CloseButtonStyle}"
                    Content="&#xE106;" Click="BtClose_Click" />

            <!--icon-->
            <TextBlock Grid.Row="0" Grid.RowSpan="2" Grid.Column="0" Margin="5 0 0 0"
                       Text="{Binding IconSource, RelativeSource={RelativeSource AncestorType={x:Type UserControl}}}"
                       FontFamily="Segoe MDL2 Assets" FontSize="28"
                       Foreground="{Binding NotificationColor.PrimaryColor, RelativeSource={RelativeSource AncestorType={x:Type UserControl}}}"
                       TextAlignment="Center" HorizontalAlignment="Center" VerticalAlignment="Center" />

            <!--message-->
            <TextBlock Grid.Row="0" Grid.Column="1" Grid.RowSpan="2" TextWrapping="Wrap" FontSize="14"
                       Margin="10 5 5 5" VerticalAlignment="Center"
                       Text="{Binding Text, RelativeSource={RelativeSource AncestorType={x:Type UserControl}}}" />

        </Grid>
    </Border>
</UserControl>